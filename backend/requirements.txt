# ==============================================================================
# AtlasOS Backend — Production Dependencies
#
# Version policy: Pin major.minor, allow patch updates.
# This balances stability with security patch adoption.
# ==============================================================================

# --- Web Framework ---
fastapi>=0.110.0,<1.0.0
uvicorn[standard]>=0.27.0,<1.0.0

# --- Database (PostgreSQL) ---
sqlalchemy[asyncio]>=2.0.25,<3.0.0
alembic>=1.13.0,<2.0.0
asyncpg>=0.29.0,<1.0.0               # Async PostgreSQL driver for SQLAlchemy
psycopg2-binary>=2.9.9,<3.0.0        # Sync PostgreSQL driver for Alembic migrations

# --- Validation & Settings ---
pydantic>=2.5.0,<3.0.0
pydantic-settings>=2.1.0,<3.0.0

# --- Redis ---
redis[hiredis]>=5.0.0,<6.0.0         # hiredis: C-based parser for faster Redis ops

# --- Task Queue ---
celery[redis]>=5.3.0,<6.0.0

# --- Vector Database ---
qdrant-client>=1.8.0,<2.0.0

# --- Authentication & Security ---
python-jose[cryptography]>=3.3.0,<4.0.0   # JWT encoding/decoding
passlib[bcrypt]>=1.7.4,<2.0.0             # Password hashing (bcrypt)

# --- HTTP Client (for embedding/NLI service calls) ---
httpx>=0.27.0,<1.0.0

# --- Logging ---
structlog>=24.1.0,<25.0.0

# --- Request Parsing ---
python-multipart>=0.0.6              # Required for form data / file uploads

# --- Validation Utilities ---
email-validator>=2.1.0,<3.0.0        # Email format validation for Pydantic

# --- Serialization ---
orjson>=3.9.0,<4.0.0                 # Fast JSON serialization for FastAPI responses
